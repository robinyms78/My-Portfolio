myjob = "hacker"

# Step through items, print each
for c in myjob:
    print(c, end = " ")

# Found
print("k" in myjob)

# Not found
print("1" in myjob)

# Substring search, no position returned
print("spam" in "abcspamdef")
